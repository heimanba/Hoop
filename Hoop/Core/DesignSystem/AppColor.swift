import SwiftUI

struct AppColor {
    let brand = Color(red: 0.94, green: 0.40, blue: 0.20)
    let brandEmphasis = Color(red: 0.86, green: 0.28, blue: 0.15)
    let success = Color(red: 0.22, green: 0.63, blue: 0.37)
    let warning = Color(red: 0.93, green: 0.63, blue: 0.16)
    let error = Color(red: 0.82, green: 0.25, blue: 0.23)
    let info = Color(red: 0.22, green: 0.57, blue: 0.89)
    let ai = Color(red: 0.12, green: 0.67, blue: 0.62)
    let training = Color(red: 0.96, green: 0.47, blue: 0.23)
    let game = Color(red: 0.26, green: 0.49, blue: 0.92)
    let duel = Color(red: 0.58, green: 0.36, blue: 0.84)
    let achievement = Color(red: 0.94, green: 0.69, blue: 0.20)
    let parent = Color(red: 0.17, green: 0.60, blue: 0.53)

    let background = Color(uiColor: .systemGroupedBackground)
    let surface = Color(uiColor: .secondarySystemGroupedBackground)
    let surfaceRaised = Color(uiColor: .systemBackground)
    let surfaceMuted = Color(uiColor: .tertiarySystemGroupedBackground)
    let fill = Color(uiColor: .secondarySystemFill)
    let fillStrong = Color(uiColor: .tertiarySystemFill)
    let border = Color(uiColor: .separator).opacity(0.22)
    let borderStrong = Color(uiColor: .separator).opacity(0.38)
    let textPrimary = Color(uiColor: .label)
    let textSecondary = Color(uiColor: .secondaryLabel)
    let textTertiary = Color(uiColor: .tertiaryLabel)
    let textOnBrand = Color.white
    let disabled = Color(uiColor: .quaternaryLabel)
}
